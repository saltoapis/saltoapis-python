# File generated by saltoapis-python. DO NOT EDIT.
[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "$(project_name)"
dynamic = ["version"]
description = "Saltoapis Python SDK"
dependencies = [
  'grpcio==$(GRPC_VERSION)',
  'grpcio-tools==$(GRPC_VERSION)',
  'protobuf==5.$(PROTOBUF_VERSION)',
]
license = {file = "LICENSE"}

[tool.setuptools]
packages = [$(packages)
]
